function setup() {
  createCanvas(windowWidth, windowHeight);
  colorMode(HSB,360,100,100);
  // setupConstants();
  // fetchHighScore();
  // setupSounds();
  // noCursor();
  // startGame();
}

function draw(){
  background(0);
  bitTextCenter("HELLO WORLD",width/2,height/2);
}